// Deprecated, types now live in content-types.ts. The JSON file is loaded at
// runtime via fs in src/lib/content.ts (server-only), so the module declaration
// for "@/data/content.json" is no longer needed.
export {};
